'use strict'

import {SERVER_API_URL} from './server-constant.config';

export const API_URL = SERVER_API_URL;

const PREFIX = 'cm/';

export const CM = {
        TOKEN_NAME: 'cmToken',
        USER_DETAILS: 'cmUser',
        PREFIX: 'cm/',
        LOGIN: 'login',
        LOGOUT: 'logout',


        // MASTERLIST
        CREW: 'crew'
    }


;