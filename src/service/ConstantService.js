'use strict'

import {SERVER_API_URL} from './ServerConstant';

export const API_URL = SERVER_API_URL;

export const
    TOKEN_NAME = 'cmToken',
    USER_DETAILS = 'cmUser',
    PREFIX = 'cm/',
    LOGIN = PREFIX + 'login'


;